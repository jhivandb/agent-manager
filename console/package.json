{
  "name": "@agent-management-platform/monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=18.20.3 <19.0.0 || >=20.14.0 <21.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "build:core-ui": "turbo run build --filter=@agent-management-platform/am-core-ui...",
    "dev": "turbo run dev --filter=web-ui --filter=@agent-management-platform/am-core-ui",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "pnpm": {
    "overrides": {
      "ws": "^8.21.1"
    }
  }
}
